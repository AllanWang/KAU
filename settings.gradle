include ':core',
        ':core-ui',

        ':about',
        ':adapter',
        ':fastadapter',
        ':colorpicker',
        ':mediapicker',
        ':kpref-activity',
        ':searchview',

        ':gradle-plugin'

if (!System.env.JITPACK)
    include ':sample'

project(":gradle-plugin").projectDir = file("buildSrc")